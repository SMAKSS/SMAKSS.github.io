import type { LanguageCodeType } from '../types/language.type';

export const DEFAULT_LANGUAGE: LanguageCodeType = 'en';

export const SUPPORTED_LANGUAGES: LanguageCodeType[] = ['en', 'fa'];

export const LANGUAGE_STORAGE_KEY = 'smakss-language';
